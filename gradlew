#!/usr/bin/env sh
# placeholder gradle wrapper script - run Gradle directly if wrapper not configured
echo "Gradle wrapper placeholder. Please use Android Studio to generate a proper wrapper or install Gradle."
